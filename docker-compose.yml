services:
  securevault:
    build: .
    container_name: securevault
    ports:
      - "5000:5000"
    volumes:
      - vault_uploads:/app/uploads
      - ./.env:/app/.env:ro
      - ./certs:/app/certs:ro
    restart: unless-stopped

volumes:
  vault_uploads:
    driver: local
